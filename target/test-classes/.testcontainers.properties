# Testcontainers configuration for Windows with Docker Desktop
testcontainers.reuse.enable=true

# Disable Ryuk if it causes issues on Windows
testcontainers.ryuk.disabled=false

# Docker host for Windows (uncomment if needed)
# docker.host=npipe:////./pipe/docker_engine

# Docker host for WSL2 (uncomment if running from WSL)
# docker.host=unix:///var/run/docker.sock
